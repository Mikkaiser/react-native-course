import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    bigFont: {
        fontSize: 34,
    },
    mainView: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        padding: '3%',
    }
});